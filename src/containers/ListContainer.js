import React from 'react';

import { getRouteProps, Link } from 'react-static';

export default getRouteProps(({ cities, states, stores }) => {

    return (
        <div>
            helloWorld
        </div>
    );
});